// DEPRECADO: este catálogo de permisos estático (placeholder de frontend) fue
// reemplazado por el catálogo real consumido desde el backend en
// `src/services/permissions.service.ts` (`GET /api/permissions`).
// Ya no se importa desde ningún lugar del proyecto. Puedes eliminar este
// archivo con seguridad (no fue posible borrarlo automáticamente desde esta
// sesión por restricciones del puente al dispositivo).
export {};
